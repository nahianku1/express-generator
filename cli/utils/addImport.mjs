import r from"fs";const o=(o,e)=>{r.readFile(o,"utf8",((t,i)=>{if(t)return void console.error("Error reading file:",t);const n=i.indexOf('import { authRouter } from "../modules/auth/auth.routes";')+57;if(-1===n)return void console.error("Couldn't find import insertion point in the source file.");const s=`import { ${e}Router } from "../modules/${e}/${e}.routes";\n`;let u=i.slice(0,n)+s+i.slice(n);r.writeFile(o,u,"utf8",(r=>{r&&console.error("Error writing file:",r)}))}))};export{o as addImport};
